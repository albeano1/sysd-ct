name: Counter Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pyinstaller

      - name: Navigate to counter/usr directory
        run: cd counter/usr

      - name: Debug repository contents
        run: |
          cd counter/usr
          ls -la
          cat Makefile

      - name: Build project
        run: |
          cd counter/usr
          make build

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install test dependencies
        run: |
          pip install pytest

      - name: Navigate to counter/usr directory
        run: cd counter/usr

      - name: Run tests
        run: |
          cd counter/usr
          make test

  package:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Navigate to counter/usr directory
        run: cd counter/usr

      - name: Build Debian package
        run: |
          cd counter/usr
          make build-deb

      - name: Upload .deb as artifact
        uses: actions/upload-artifact@v4
        with:
          name: counter-deb-package
          path: counter/usr/counter_service-v2.0.0.deb  # Update path to include /counter/usr

  lint:
    runs-on: ubuntu-latest
    needs: package
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Install lintian
        run: |
          sudo apt-get update
          sudo apt-get install -y lintian

      - name: Navigate to counter/usr/local/bin directory
        run: cd counter/usr/local/bin

      - name: Lint Debian package
        run: |
          cd counter/usr/local/bin
          make lint-deb